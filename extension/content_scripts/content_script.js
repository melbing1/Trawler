var backgroundService = browser.extension.getBackgroundPage();

backgroundService.alertUser("hello", "This is the msg", true);
backgroundService.getRegistrationOf(domain, compareTo, WhoisDataProcessing, handleRequestRejection); //If the user wants to continue, the heuristic check is performed
