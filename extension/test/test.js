let assert = require('assert');
describe('Array', function () {
    describe('#indexOf()', function () {
        it('Should return 01 when the value is not present', function () {
            assert( 1 === 1)
        });
    });
});